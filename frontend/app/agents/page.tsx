"use client";
import {useEffect,useState} from "react"; import {api} from "@/lib/api";
type Run={id:number;run_id:string;week:string;agent_name:string;status:string;error:string;started_at:string;finished_at:string|null};
export default function Agents(){const [rows,setRows]=useState<Run[]>([]); useEffect(()=>{api<Run[]>("/api/agents/runs").then(setRows)},[]); return <div><h1 className="mb-6 text-2xl font-bold">Agent 运行状态</h1><div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-2">周次</th><th>Agent</th><th>状态</th><th>开始时间</th><th>错误</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-b last:border-0"><td className="p-2">{r.week}</td><td>{r.agent_name}</td><td><span className={r.status==="success"?"text-green-600":r.status==="failed"?"text-red-600":"text-amber-600"}>{r.status}</span></td><td>{new Date(r.started_at).toLocaleString()}</td><td className="max-w-xs truncate text-red-600">{r.error}</td></tr>)}</tbody></table></div></div>}
